// TODO(Phase 3): capture + compress (max 1280px edge, JPEG q70, EXIF
// stripped except GPS/time) via image_picker + flutter_image_compress.
// Files live in the app documents dir until uploaded, never in gallery.
class ImageService {}
