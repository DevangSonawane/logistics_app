// TODO(Phase 3): speech_to_text with the chosen-language locale and the
// small per-language phrase set (confirmation chip before applying),
// plus record for voice notes behind a consent screen.
class VoiceService {}
